# Core
whisperx>=3.8
PyAudioWPatch>=0.2.12; sys_platform == 'win32'  # Mikrofon + WASAPI-Loopback (nur Windows)
sounddevice>=0.4; sys_platform != 'win32'       # Mikrofon (macOS/Linux) - kein System-Audio
soundfile>=0.13
scipy
numpy
python-dotenv
PySide6>=6.7          # GUI (gui_qt.py)
psutil
av                    # Video-container audio extraction (MKV, MP4, ...)
requests              # KIT ToolBox (Server) remote transcription

# PyTorch mit CUDA 12.8 (separat installieren, siehe README)
# torch>=2.11
# torchaudio>=2.11
# torchvision>=0.26
