# Core
whisperx>=3.8
PyAudioWPatch>=0.2.12; sys_platform == 'win32'  # microphone + WASAPI loopback (Windows only)
sounddevice>=0.4; sys_platform != 'win32'       # microphone (macOS/Linux) - no system audio
soundfile>=0.13
scipy
numpy
python-dotenv
PySide6>=6.7          # GUI (gui_qt.py)
psutil
av                    # video-container audio extraction (MKV, MP4, ...)
requests              # remote provider transcription

# PyTorch with CUDA 12.8 (install separately, see README)
# torch>=2.11
# torchaudio>=2.11
# torchvision>=0.26
