"""VoxScribe backend package: recording, transcription, providers, hardware
detection, speaker profiles, and the model-download/build-exe scripts.

`main.py`/`gui_qt.py` in the project root are thin CLI/GUI entry points that
import from here; `qt_app/` (GUI pages) also imports from this package.
"""
